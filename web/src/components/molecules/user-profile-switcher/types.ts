/**
 * Props for the UserProfileSwitcher component.
 * @interface UserProfileSwitcherProps
 * 
 * @property {boolean} isCollapsed - Indicates if the profile switcher is in a collapsed state.
 */
export interface UserProfileSwitcherProps {
  isCollapsed: boolean;
}
