# Run Units Tests Action

This custom GitHub Action is designed to run units tests for each part of the project. It ensures that we test our code and that it works as intended.

## Features

This action performs the following checks:
- **Test**: Ensures the code's tests are working and succeeding. It first setup node because it is needed to run the tests.
